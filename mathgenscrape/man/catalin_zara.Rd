% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{catalin_zara}
\alias{catalin_zara}
\title{Catalin Zara's advisor tree}
\format{
A "mathgen_df" object (see \code{\link[=mathgen_traverse]{mathgen_traverse()}}): a data.frame
of 299 edges (\code{from}/\code{to} ids, oriented advisor -> student), with the
243-row vertex table and traversal metadata attached as attributes
\code{"vertices"}, \code{"start_id"} (47185), and \code{"direction"} ("advisors").
}
\source{
\url{https://genealogy.math.ndsu.nodak.edu/id.php?id=47185},
fetched with \code{get_advisors(id = 47185)}.
}
\usage{
catalin_zara
}
\description{
An example "mathgen_df" object: the recursive advisor tree for Catalin
Zara (Math Genealogy Project id 47185), as returned by
\code{get_advisors(id = 47185)}. Larger and more densely interconnected than
\link{ethan_bolker}, with several vertices having 2-3 recorded advisors, so
it's a useful stress test for layout/rendering (e.g. it shows how
\code{method = "graphviz-dot"} and \code{method = "igraph"} can place the same
vertex at visually different "generations" when its shortest and
longest path back from the root differ -- not a data error, just what
happens when a DAG this convergent is forced into a layered layout).
Included for convenience in examples, \code{plot()}/\code{print()}
demonstrations, and vignettes, so they don't depend on a live,
sometimes-slow network call to the Math Genealogy Project.
}
\examples{
print(catalin_zara)
plot(catalin_zara)
}
\keyword{datasets}
