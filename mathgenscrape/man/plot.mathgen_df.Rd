% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.mathgen_df}
\alias{plot.mathgen_df}
\title{Plot a mathgen_df advisor/student graph}
\usage{
\method{plot}{mathgen_df}(
  x,
  method = c("igraph", "graphviz-dot", "visNetwork"),
  root = attr(x, "start_id"),
  maxdepth = Inf,
  minyear = -Inf,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{a "mathgen_df" object}

\item{method}{one of "igraph" (default), "graphviz-dot", or "visNetwork"}

\item{root}{id of the vertex to root the tree layout at; defaults to
the id the traversal was started from (\code{attr(x, "start_id")})}

\item{maxdepth}{maximum number of generations from \code{root} to include
(Inf, the default, includes everything)}

\item{minyear}{earliest year to include (-Inf, the default, includes
everything); vertices with no recorded year are kept unless doing so
would leave them disconnected from \code{root} (see Details)}

\item{main}{plot title; defaults to "Advisor tree: \if{html}{\out{<name>}}" or "Student
tree: \if{html}{\out{<name>}}" depending on the traversal direction}

\item{...}{additional arguments passed on to \code{igraph::plot.igraph()};
only used when \code{method = "igraph"}}
}
\value{
depends on \code{method}: the \code{igraph} graph object, invisibly, for
\code{"igraph"}; the path to the rendered SVG file, invisibly, for
\code{"graphviz-dot"} (which is also opened in a browser); the
\code{visNetwork} htmlwidget (visibly, so it displays) for \code{"visNetwork"}.
These three are not interchangeable -- a base-graphics side effect, a
file path, and an auto-printing htmlwidget -- so code that scripts
against the return value of \code{plot()} (rather than just calling it for
its display side effect) will need to branch on \code{method}.
}
\description{
Plots the tree returned by \code{\link[=mathgen_traverse]{mathgen_traverse()}}, \code{\link[=get_advisors]{get_advisors()}}, or
\code{\link[=get_students]{get_students()}}, rooted at the traversal's starting id. Three
rendering backends are available:
}
\details{
\itemize{
\item \code{"igraph"} (default): a quick base-graphics plot via
\code{igraph::plot.igraph()}. No extra system dependencies, but its tree
layout crowds badly past a few dozen nodes.
\item \code{"graphviz-dot"}: calls the system \code{dot} command directly (\emph{not} via
the DiagrammeR/Rgraphviz packages) for a proper layered DAG layout --
by far the prettiest of the three, and true vector SVG. It has no
pan/zoom of its own, so the result is opened in a browser at native
resolution, where scrolling and the browser's own zoom stay crisp.
Requires a system Graphviz install (the \code{dot} binary). DiagrammeR's
\code{grViz()} is avoided deliberately: it unconditionally replaces every
apostrophe with a double quote before rendering, which silently
corrupts real names like "d'Alembert" into invalid DOT syntax.
\item \code{"visNetwork"}: an interactive htmlwidget (vis.js) with native
pan/zoom/drag. CRAN-only install, no system dependency. Its
auto-layout is a bit busier than dot's, but it's the only one of the
three that's actually explorable.
}

\code{maxdepth} and \code{minyear} subset an already-fetched tree for plotting,
without re-fetching: \code{maxdepth} keeps only vertices within that many
generations of \code{root} (following the same advisor/student direction the
tree was traversed in), and \code{minyear} drops vertices whose recorded
year is earlier than it (vertices with no recorded year are always
kept on the year criterion alone, since it isn't known whether they'd
violate the cutoff). Whatever remains is then pruned to only what's
still connected to \code{root}: keeping every NA-year vertex regardless of
\code{minyear} can otherwise leave one stranded once the (dated, and so
excluded) ancestor connecting it back to \code{root} is cut, and a
disconnected vertex has no principled place in a tree layout --
\code{layout_as_tree()} was observed dumping every such orphan at the same
depth as \code{root}'s immediate advisor, reading as a large, spurious
fan-out one generation down that wasn't really there.
}
