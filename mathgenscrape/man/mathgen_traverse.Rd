% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traverse.R
\name{mathgen_traverse}
\alias{mathgen_traverse}
\title{Recursively traverse the advisor or student graph}
\usage{
mathgen_traverse(
  start_id,
  direction = c("advisors", "students"),
  include_mentors = TRUE,
  max_nodes = 500,
  max_depth = Inf,
  delay = 0.2,
  timeout = 30,
  retries = 1,
  quiet = FALSE
)
}
\arguments{
\item{start_id}{starting Math Genealogy Project id}

\item{direction}{"advisors" to walk backward toward ancestors, or
"students" to walk forward toward descendants}

\item{include_mentors}{if TRUE (the default), a person with no formal
advisor listed but a "Mentor" recorded instead (e.g. Andrew Mattei
Gleason, id 13307, mentored by George Mackey) has that mentor treated
as their advisor. Only affects \code{direction = "advisors"}; set to FALSE
to only ever follow genuine Advisor relationships.}

\item{max_nodes}{maximum number of profile pages to fetch before
stopping (returns a partial graph with a warning if reached)}

\item{max_depth}{maximum number of generations to recurse (Inf for
unlimited, subject to \code{max_nodes})}

\item{delay}{seconds to pause between page fetches (politeness/rate-limiting)}

\item{timeout}{request timeout in seconds, passed to the underlying fetch}

\item{retries}{number of retries per page fetch on failure}

\item{quiet}{if FALSE, warn/message about failures, truncation, and progress}
}
\value{
a "mathgen_df" object: a data.frame of edges (\code{from}/\code{to} ids,
oriented advisor -> student) that also behaves as a plain data.frame
(print, subset, \code{write.csv()}, ...). The vertex table (\code{id}/\code{name}/
\code{school}/\code{year}/\code{visited}) and traversal metadata are attached as
attributes \code{"vertices"}, \code{"start_id"}, and \code{"direction"}; see
\code{\link[=plot.mathgen_df]{plot.mathgen_df()}} for a ready-made plot method.
}
\description{
BFS over id.php profile pages, following either the advisor links
(\code{direction = "advisors"}, i.e. walking backward toward ancestors) or
the student links (\code{direction = "students"}, walking forward toward
descendants). Regardless of direction, edges are always returned as
advisor_id -> student_id, so the two directions produce graphs that
compose consistently. \code{\link[=get_advisors]{get_advisors()}} and \code{\link[=get_students]{get_students()}} are thin
wrappers around this function and are usually more convenient to call
directly.
}
\details{
Nodes can have more than one advisor (e.g. Felix Klein was supervised by
both Rudolf Lipschitz and Julius Plücker), so both branches are always
followed; a visited/queued set prevents re-fetching or infinite loops
where lineages reconverge on a shared ancestor/descendant.

Descendant trees in particular can be enormous (some 19th-century
mathematicians have 100,000+ recorded descendants), so traversal is
capped by \code{max_nodes} and/or \code{max_depth}; hitting either cap yields a
partial graph plus a warning rather than an unbounded crawl.
}
