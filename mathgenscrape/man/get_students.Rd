% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traverse.R
\name{get_students}
\alias{get_students}
\title{Trace a mathematician's students, recursively}
\usage{
get_students(
  id = NULL,
  name = NULL,
  max_nodes = 500,
  max_depth = Inf,
  delay = 0.2,
  timeout = 30,
  retries = 1,
  quiet = FALSE
)
}
\arguments{
\item{id}{starting Math Genealogy Project id. If \code{NULL}, \code{name} is used
instead to resolve one via \code{\link[=mathgen_lookup]{mathgen_lookup()}}. A non-numeric string
passed positionally as \code{id} (e.g. \code{get_advisors("Catalin Zara")}) is
treated as \code{name} rather than failing, since ids on this site are
always numeric.}

\item{name}{a name to look up via \code{\link[=mathgen_lookup]{mathgen_lookup()}} when \code{id} is not
supplied. Ignored if \code{id} is given. Fails if the lookup has zero hits,
or more than one (the error message lists the matching ids so you can
retry with \code{id} directly).}

\item{max_nodes}{maximum number of profile pages to fetch before
stopping (returns a partial graph with a warning if reached)}

\item{max_depth}{maximum number of generations to recurse (Inf for
unlimited, subject to \code{max_nodes})}

\item{delay}{seconds to pause between page fetches (politeness/rate-limiting)}

\item{timeout, retries, quiet}{passed to the underlying fetch; see \code{\link[=mathgen_traverse]{mathgen_traverse()}}}
}
\value{
a "mathgen_df" object; see \code{\link[=mathgen_traverse]{mathgen_traverse()}} for details and
\code{\link[=plot.mathgen_df]{plot.mathgen_df()}} for a ready-made plot method.
}
\description{
Starting from a Math Genealogy Project id, walks forward through student
links to build the descendant tree (students, their students, and so
on). Descendant trees can be very large -- some historical
mathematicians have well over 100,000 recorded descendants -- so
\code{max_nodes}/\code{max_depth} matter much more here than for \code{\link[=get_advisors]{get_advisors()}};
consider setting them explicitly rather than relying on the defaults for
anyone with a long academic lineage. A thin wrapper around
\code{\link[=mathgen_traverse]{mathgen_traverse()}}.
}
